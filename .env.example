# Example environment variables for local development
# Copy this to .env.local or .env and fill in values.

# Frontend (safe to use in browser builds)
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# Backend (server-only - never commit or expose these to frontend)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Optional: other public vars
VITE_PUBLIC_BUILDER_KEY=replace-me

# Notes:
# - `VITE_` prefixed variables are injected into the client bundle by Vite.
# - `SUPABASE_SERVICE_ROLE_KEY` is server-only and must never be present in client
#   code, client-side bundles, or committed to version control.
# - Use `.env.local` or platform secrets for sensitive keys in CI/deployments.

NODEMAILER_EMAIL=sahilranjan017@gmail.com
NODEMAILER_PASSWORD=onjpjhmxdomyeoal 
NODEMAILER_FROM=noreply@agrobuild.com